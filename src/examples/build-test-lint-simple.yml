description: |
  Build, test, and lint a single-resolver project

usage:
  version: 2.1

  orbs:
    stack-build: pbrisbin/stack-build@x.y

  workflows:
    commit:
      jobs:
        - stack-build/build-test-lint:
            name: build
